@model IEnumerable<User>

<style>
    .table {
        border-collapse: collapse;
    }

        .table th, .table td {
            padding: 8px; /* Add padding for better readability */
        }
</style>

<table class="table">
    <tr>
        <th><a href="javascript:void(0);" id="userIDHeader">User ID</a></th>
        <th>First Name</th>
        <th><a href="javascript:void(0);" id="lastNameHeader">Last Name</a></th>
        <th>Email Address</th>
        <th>Phone Number</th>
        <th><a href="javascript:void(0);" id="eventIDHeader">Event ID</a></th>
        <th>Event Name</th>
    </tr>

    @foreach (var user in @Model)
    {
        <tr>
            <td><a href=/User/ViewUser/@user.UserID>@user.UserID</a></td>
            <td>@user.FirstName</td>
            <td>@user.LastName</td>
            <td>@user.EmailAddress</td>
            <td>@user.PhoneNumber</td>
            <td>@user.EventID</td>
            <td>@user.EventName</td>
        </tr>
    }

</table>

<input type="hidden" id="sortOrder" value="asc" />

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>

<script>// Function to toggle sorting order and update table
    function sortTableUserID(columnName) {
        // Retrieve the current sorting order from the header text
        var currentOrder = $('#userIDHeader').text().includes('▼') ? 'desc' : 'asc';

        // Make AJAX request to toggle sorting order
        $.ajax({
            url: '/User/SortUsers',
            data: { column: columnName, sortOrder: currentOrder },
            type: 'GET',
            success: function (data) {
                // Replace table content with sorted data
                $('.table tbody').html(data);

                // Update UserID header to indicate sorting order
                $('#userIDHeader').text('User ID ' + (currentOrder === 'asc' ? '▼' : '▲'));
            },
            error: function () {
                alert('Error occurred while sorting.');
            }
        });
    }

    // Continuous sorting loop when User ID hyperlink is clicked
    $(document).on('click', '#userIDHeader', function () {
        // Call sortTableUserID function with 'UserID' column
        sortTableUserID('UserID');
    });</script>

<script>// Function to toggle sorting order and update table
    function sortTableLastName(columnName) {
        // Retrieve the current sorting order from the header text
        var currentOrder = $('#lastNameHeader').text().includes('▼') ? 'desc' : 'asc';

        // Make AJAX request to toggle sorting order
        $.ajax({
            url: '/User/SortUsers',
            data: { column: columnName, sortOrder: currentOrder },
            type: 'GET',
            success: function (data) {
                // Replace table content with sorted data
                $('.table tbody').html(data);

                // Update LastName header to indicate sorting order
                $('#lastNameHeader').text('Last Name ' + (currentOrder === 'asc' ? '▼' : '▲'));
            },
            error: function () {
                alert('Error occurred while sorting.');
            }
        });
    }

    // Continuous sorting loop when LastName hyperlink is clicked
    $(document).on('click', '#lastNameHeader', function () {
        // Call sortTableLastName function with 'LastName' column
        sortTableLastName('LastName');
    });</script>

<script>// Function to toggle sorting order and update table
    function sortTable(columnName) {
        // Retrieve the current sorting order from the header text
        var currentOrder = $('#eventIDHeader').text().includes('▼') ? 'desc' : 'asc';

        // Make AJAX request to toggle sorting order
        $.ajax({
            url: '/User/SortUsers',
            data: { column: columnName, sortOrder: currentOrder },
            type: 'GET',
            success: function (data) {
                // Replace table content with sorted data
                $('.table tbody').html(data);

                // Update EventID header to indicate sorting order
                $('#eventIDHeader').text('Event ID ' + (currentOrder === 'asc' ? '▼' : '▲'));
            },
            error: function () {
                alert('Error occurred while sorting.');
            }
        });
    }

    // Continuous sorting loop when EventID hyperlink is clicked
    $(document).on('click', '#eventIDHeader', function () {
        // Call sortTable function with 'EventID' column
        sortTable('EventID');
    });</script>