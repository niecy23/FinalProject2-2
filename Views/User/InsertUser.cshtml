@model User

<h2>Create a New User</h2>

@using (Html.BeginForm("InsertUserToDatabase", "User", FormMethod.Post))
{
    <input asp-for="UserID" type="hidden" value="" />

    <label asp-for="FirstName" class="control-label">First Name</label>
    <input asp-for="FirstName" class="form-control" />

    <label asp-for="LastName" class="control-label">Last Name</label>
    <input asp-for="LastName" class="form-control" />

    <label asp-for="EmailAddress" class="control-label">Email Address</label>
    <input asp-for="EmailAddress" class="form-control" />

    @*<label asp-for="PhoneNumber" class="control-label">Phone Number</label>
        <input asp-for="PhoneNumber" class="form-control" placeholder="(123) 456-7890" pattern="\(\d{3}\) \d{3}-\d{4}" />*@

    <label asp-for="PhoneNumber" class="control-label">Phone Number</label>
    <input id="PhoneNumberInput" asp-for="PhoneNumber" class="form-control" placeholder="(123) 456-7890" pattern="\(\d{3}\) \d{3}-\d{4}" />

    <input type="hidden" id="ActualPhoneNumber" asp-for="PhoneNumber" />

    <div class="form-group">
        <label for="">Select an Event</label>
        <select class="form-control" name="EventID" value="">
            @foreach (var instance in Model.EventsData)
            {
                <option value="@instance.EventID">@instance.EventID @instance.EventName</option>
            }
        </select>
    </div>

    <input type="submit" value="Create User" class="btn btn-primary" />
}

@section scripts {
    <script>document.addEventListener("DOMContentLoaded", function () {
            var phoneNumberInput = document.getElementById("PhoneNumberInput");
            var actualPhoneNumberInput = document.getElementById("ActualPhoneNumber");

            phoneNumberInput.addEventListener("input", function () {
                // Remove non-numeric characters
                var numericValue = this.value.replace(/\D/g, "");

                // Format the phone number
                var formattedValue = formatPhoneNumber(numericValue);

                // Update the visible input field
                this.value = formattedValue;

                // Update the hidden input field with the actual phone number value
                actualPhoneNumberInput.value = numericValue;
            });

            function formatPhoneNumber(value) {
                var formattedValue = value.replace(/(\d{3})(\d{3})(\d{4})/, "($1) $2-$3");
                return formattedValue;
            }
        });</script>
}

@*<label asp-for="FirstName" class="control-label">First Name</label>
    <input type="text" name="FirstName" class="form-control" value="" />

    <label asp-for="LastName" class="control-label">Last Name</label>
    <input type="datetime" name="LastName" class="form-control" value="" />

    <label asp-for="EmailAddress" class="control-label">Email Address</label>
    <input type="text" name="EmailAddress" class="form-control" value="" />

    <label asp-for="PhoneNumber" class="control-label">Phone Number</label>
    <input type="text" name="PhoneNumber" class="form-control" value="" />*@

@*<label asp-for="PhoneNumber" class="control-label">Phone Number</label>
    <input asp-for="PhoneNumber" class="form-control" />*@